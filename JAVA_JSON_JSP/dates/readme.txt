
Prototyping sample similar to task given at my current job. (partial code)

Retrieves documents and displays them through tabs similar to firefox. 
Each tab is generated using the files name that are stored on a remote 
server. 

This program using ajax and json to parse the given file names . Onces the files
names and url have been stored in JSON.

The tab component will be created for each document using the tab name and the link for displaying the document.
(in date.jsp tabs are created)
